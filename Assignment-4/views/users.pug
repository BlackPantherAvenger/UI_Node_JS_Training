extends layouts/main-layout.pug
block content
    main
        if usersData.length > 0
            ul
                each user in usersData
                    li #{user.username}
        else
            <p>No Users